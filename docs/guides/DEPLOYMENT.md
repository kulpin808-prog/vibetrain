> **Актуальная навигация:** [оглавление документации](../README.md). Ключевое по деплою — [RUNBOOK](../RUNBOOK.md).

# 🚀 Развертывание Fitness Trainer AI Bot

## 📋 Текущая ситуация

Сейчас бот работает **ТОЛЬКО** на вашем компьютере в режиме разработки (polling). Когда вы выключаете компьютер - бот перестает работать.

## 🎯 Цель развертывания

Бот должен работать **24/7** на сервере, чтобы пользователи могли обращаться к нему в любое время.

## 🆓 БЕСПЛАТНЫЕ варианты развертывания

### 1. **Railway** (Рекомендую - самый простой)

**Плюсы:**
- Бесплатный план (512MB RAM, 1GB storage)
- Автоматическое развертывание из GitHub
- PostgreSQL база данных включена
- Простая настройка

**Минусы:**
- Ограничения по ресурсам (может не хватить для большого количества пользователей)

**Инструкция:**
1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Подключите GitHub аккаунт
3. Создайте новый проект из репозитория
4. Добавьте переменные окружения (см. ниже)
5. Разверните проект

### 2. **Render** (Бесплатный с ограничениями)

**Плюсы:**
- Бесплатный tier (750 часов/месяц)
- Простая настройка
- Поддержка Node.js

**Минусы:**
- Спит после 15 минут без активности
- Ограниченное время работы

### 3. **Vercel** (Бесплатный)

**Плюсы:**
- Бесплатный план
- Быстрое развертывание
- Хорошая документация

**Минусы:**
- Serverless функции (не подходит для постоянного бота)
- Таймауты на запросы

## 💰 ПЛАТНЫЕ варианты ($5-20/месяц)

### **DigitalOcean App Platform** ($5/месяц)
- Стабильная работа
- Легко масштабировать
- PostgreSQL база данных

### **Heroku** ($7/месяц)
- Простая настройка
- PostgreSQL addon
- Хорошая документация

### **VPS сервер** ($10-20/месяц)
- Полный контроль
- DigitalOcean, Linode, или Hetzner
- Нужно настраивать самому

## ⚙️ Настройка переменных окружения

Создайте `.env` файл на сервере:

```bash
# Production settings
NODE_ENV=production
PORT=3000
WEBHOOK_URL=https://your-app-domain.com

TELEGRAM_BOT_TOKEN=из_BotFather
OPENAI_API_KEY=из_OpenAI
# Тот же публичный URL, что выдал хостинг (HTTPS), без слэша в конце
TELEGRAM_WEBAPP_URL=https://ваш-сервис.up.railway.app
WEBAPP_URL=https://ваш-сервис.up.railway.app

# Add these when ready
GOOGLE_SHEETS_API_KEY=your_key_here
GOOGLE_SHEETS_SPREADSHEET_ID=your_id_here
NOTION_API_KEY=your_key_here
NOTION_DATABASE_ID=your_id_here
```

## 🛠️ Быстрое развертывание на Railway

### Переменные в Railway (обязательно до первого успешного деплоя)

В панели: **сервис** → вкладка **Variables** → **New Variable**. Файла `.env` на Railway нет — только этот список.

| Имя | Значение |
|-----|----------|
| `TELEGRAM_BOT_TOKEN` | Токен бота из @BotFather |
| `OPENAI_API_KEY` | Ключ с [platform.openai.com](https://platform.openai.com) |
| `NODE_ENV` | `production` |

После **Settings → Networking → Generate Domain** добавьте (один и тот же HTTPS URL, **без** слэша в конце):

| Имя | Значение |
|-----|----------|
| `WEBAPP_URL` | `https://ваш-поддомен.up.railway.app` |
| `TELEGRAM_WEBAPP_URL` | тот же URL |

Сохраните — Railway пересоберёт сервис. В логах не должно остаться ошибок про `TELEGRAM_BOT_TOKEN`.

**Про предупреждение npm:** `npm warn config production Use --omit=dev instead` — это не ошибка деплоя, а замечание npm при `NODE_ENV=production`. На работу бота обычно не влияет, можно игнорировать.

### Тестовая среда на GitHub (черновая разработка → прод)

Цель: править код в «песочнице», а в прод (`main` + Railway) попадает только проверенное.

**Вариант 1 — ветка `develop` в том же репозитории (удобнее всего)**

1. Создать ветку и пушить черновики только в неё:
   ```bash
   git checkout -b develop
   git push -u origin develop
   ```
2. Готовый функционал — **Pull Request** `develop` → `main`, мерж после ревью.
3. Railway оставьте на **ветке `main`** (прод). При желании второй проект Railway подключите к **`develop`**, отдельный домен, **отдельный тестовый бот** и свои Variables — так не трогаете прод-бота.

**Вариант 2 — отдельный репозиторий** (например `kools-dev`)

- Дублируете репозиторий или делаете fork, ведёте там эксперименты; стабильные коммиты переносите в основной репо (`cherry-pick` или ручное копирование).
- Плюс: полная изоляция; минус: два репозитория и синхронизация.

**Практический совет:** для Kools достаточно **`main` + `develop`** в [kulpin808-prog/vibetrain](https://github.com/kulpin808-prog/vibetrain), прод-Railway только с `main`. Локально и на втором Railway (опционально) тестируете ветку `develop`.

### Шаг 1: Подготовьте код
```bash
# Добавьте эти файлы в git
git add .
git commit -m "Add production deployment files"
git push origin main
```

### Шаг 2: Создайте Railway аккаунт
1. Перейдите на [railway.app](https://railway.app)
2. Зарегистрируйтесь (GitHub аккаунт)
3. Подтвердите email

### Шаг 3: Разверните проект
1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Выберите ваш репозиторий
4. Railway автоматически определит Node.js проект

### Шаг 3.1: Команда запуска
В настройках сервиса укажите **Start Command**:

`npm run start:full`

Так на одном порту поднимается и **Web App** (`src/server.js`), и **бот** (`src/index.js`, long polling).

### Шаг 4: Настройте переменные
В Railway dashboard:
1. Перейдите в "Variables"
2. Добавьте все переменные из `.env`
3. Добавьте `WEBHOOK_URL` (Railway предоставит домен)

### Шаг 5: Настройте webhook в Telegram
```bash
# После развертывания получите URL от Railway
# И настройте webhook:
curl -F "url=https://your-railway-app.com" https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook
```

## 📊 Мониторинг и поддержка

### Railway бесплатный план:
- **512 MB RAM** - хватит для 50-100 активных пользователей
- **1 GB Storage** - достаточно для базы данных
- **Автоматическое масштабирование**

### Когда переходить на платный хостинг:
- >100 активных пользователей в день
- Нужна кастомная база данных
- Требуется высокая надежность

## 🐛 Устранение проблем

### Бот не отвечает:
1. Проверьте логи в Railway dashboard
2. Убедитесь, что переменные окружения правильные
3. Проверьте webhook URL

### Ошибка памяти:
- Перейдите на платный план Railway
- Или оптимизируйте код (кеширование, etc.)

### Таймауты:
- Увеличьте таймауты в настройках
- Оптимизируйте AI запросы

## 📞 Поддержка

Если возникнут проблемы:
1. Проверьте логи приложения
2. Посмотрите документацию Railway
3. Напишите в чат поддержки Railway

## 🎯 Следующие шаги после развертывания

1. **Тестирование:** Убедитесь, что бот работает
2. **Мониторинг:** Настройте логи и алерты
3. **Резервное копирование:** Настройте бэкапы базы данных
4. **Масштабирование:** Мониторьте использование ресурсов
